<Page
	x:Class="Anilibria.HomeView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:onlineplayer="using:Anilibria.Pages.OnlinePlayer"
	xmlns:viewmodel2="using:Anilibria.Pages.Releases"
	xmlns:viewmodel="Anilibria.Pages.HomePage"
	mc:Ignorable="d"
	d:DataContext="{d:DesignInstance viewmodel:HomeViewModel}">

	<Grid>
		<SplitView
			IsPaneOpen="True"
			DisplayMode="Overlay"
			OpenPaneLength="260">
			<SplitView.Pane>
				<RelativePanel
					Background="#D3D3D3"
					BorderBrush="#881919"
					BorderThickness="0 0 2 0">
					<ListView
						RelativePanel.AlignTopWithPanel="True"
						RelativePanel.AlignRightWithPanel="True"
						RelativePanel.AlignLeftWithPanel="True"
						HorizontalAlignment="Stretch"
						ItemsSource="{Binding Items}">
						<ListView.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
									<TextBlock
										Text="{Binding Icon}"
										FontFamily="Segoe MDL2 Assets"
										FontSize="30"
									/>
									<TextBlock
										Text="{Binding Name}"
										VerticalAlignment="Center"
										Margin="10 0 0 0"
									/>
								</StackPanel>
							</DataTemplate>
						</ListView.ItemTemplate>
					</ListView>
					<Grid
						RelativePanel.AlignBottomWithPanel="True"
						RelativePanel.AlignRightWithPanel="True"
						RelativePanel.AlignLeftWithPanel="True"
						Margin="8 0 0 4">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto" />
							<ColumnDefinition />
						</Grid.ColumnDefinitions>
						<Image
							Height="50"
							Stretch="Uniform"
							Source="/Assets/anilibriaicon.png"
						/>
						<StackPanel
							Orientation="Vertical"
							VerticalAlignment="Center"
							Grid.Column="1"
							Margin="8 0 0 0">
							<TextBlock>Клиент для сайта Anilibria</TextBlock>
							<TextBlock>
								<Run>версия</Run>
								<Run Text="{Binding Version}" />
							</TextBlock>
						</StackPanel>
					</Grid>
				</RelativePanel>
			</SplitView.Pane>
			<!--<viewmodel:OnlinePlayerView></viewmodel:OnlinePlayerView>-->
			<viewmodel2:ReleasesView></viewmodel2:ReleasesView>
		</SplitView>
	</Grid>
</Page>
